[package]
name = "pi-host-tools"
version = "0.1.0"
edition = "2021"
description = "Agent 工具的本机实现：workspace 越狱保护 + 写前备份/回滚（可被任意宿主复用）"
license = "MIT"
publish = false

[dependencies]
regex = "1"
serde_json = "1"
